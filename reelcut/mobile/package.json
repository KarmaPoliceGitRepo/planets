{
  "name": "reelcut-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "ReelCut mobile client (iOS + Android) — P6. Reads the portable project doc and renders locally via ffmpeg-kit.",
  "scripts": {
    "start": "react-native start",
    "ios": "react-native run-ios",
    "android": "react-native run-android",
    "tsc": "tsc --noEmit"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-native": "0.74.0",
    "ffmpeg-kit-react-native": "^6.0.2",
    "react-native-document-picker": "^9.1.0",
    "react-native-fs": "^2.20.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/react": "^18.2.0"
  }
}
